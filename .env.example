# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# QuickBooks Online (developer.intuit.com → your app → Keys & credentials)
# Use Development keys + sandbox for testing; Production keys for your real company (see docs/QBO_CONNECT_PRODUCTION.md)
VITE_QB_CLIENT_ID=your-client-id
VITE_QB_REDIRECT_URI=http://localhost:5173/qb-callback
# Optional: set to true when using Production keys so the QB card shows "Production" (Supabase must have QB_USE_PRODUCTION=true too)
# VITE_QB_USE_PRODUCTION=false
# Optional in frontend; NEVER commit real secret. Supabase Edge Functions need QB_CLIENT_SECRET + QB_USE_PRODUCTION=true for production.
# VITE_QB_CLIENT_SECRET=
